{
  "name": "the-nest",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --test tests/*.test.js && node scripts/check-static-app.js"
  }
}
