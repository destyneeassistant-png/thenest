(() => { const KEY='nest-dissertation-v1'; const form=document.getElementById('dissertation-form'); const status=document.getElementById('save-status'); const saved=JSON.parse(localStorage.getItem(KEY)||'{}'); Object.entries(saved).forEach(([key,value])=>{if(form.elements[key]) form.elements[key].value=value;}); const renderLinks=()=>{const urls=form.elements.relatedLinks.value.split(/\n/).map(v=>v.trim()).filter(v=>/^https?:\/\//.test(v)); document.getElementById('related-links').innerHTML=urls.map(url=>`<a href="${url.replace(/"/g,'%22')}" target="_blank" rel="noopener">${url}</a>`).join('');}; renderLinks(); form.onsubmit=e=>{e.preventDefault(); const data=Object.fromEntries(new FormData(form)); localStorage.setItem(KEY,JSON.stringify(data)); renderLinks(); status.textContent='Saved locally';}; let timer; form.oninput=()=>{status.textContent='Unsaved changes'; clearTimeout(timer); timer=setTimeout(()=>form.requestSubmit(),800);}; })();
